#include "topology.h"

#include <iostream>

void CompactTopology::Dump(std::ostream& out) const {
//    graph::Topology topology;
//    *topology.mutable_parents() = {parents_.begin(), parents_.end()};
//    *topology.mutable_sizes() = {sizes_.begin(), sizes_.end()};
//    topology.SerializeToOstream(&out);
}

void CompactTopology::Load(std::istream& in) {
//    graph::Topology topology;
//    topology.ParseFromIstream(&in);
//    parents_ = {topology.parents().begin(), topology.parents().end()};
//    sizes_ = {topology.sizes().begin(), topology.sizes().end()};
}
